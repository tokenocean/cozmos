<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <style>
      body {
        margin: 0;
        font-family: sans-serif;
        color: #eeeeee;
        background-color: #2d2e32;
      }

      p {
        line-height: 1.7;
      }

      .container {
        width: 600px;
        margin: 0 auto;
        background-color: #2d2e32;
        padding-top: 60px;
      }

      .logo {
        width: 100%;
        margin: 0 auto;
        text-align: center;
      }

      .logo img {
        height: 40px;
        margin: 0 auto;
      }

      .link {
        display: block;
        margin-top: 30px;
        color: #52b9ff;
        text-decoration: none;
      }

      hr {
        border-color: whitesmoke;
        margin-bottom: 30px;
      }

      .confirm-container {
        display: flex;
        justify-content: flex-end;
      }

      .confirm {
        display: flex;
        align-items: center;
        justify-content: center;
        width: 120px;
        margin: 40px 0;
        text-decoration: none;
        border-radius: 30px;
        padding: 12px 24px !important;
        color: #444444;
        font-weight: bold;
        background: #00eaaf;
        background: linear-gradient(
          90deg,
          #fa7900 0%,
          #df36b4 43%,
          #0063ea 77%,
          #00eaaf 100%
        );
        border: 0px solid white;
      }

      .footer p {
        font-size: 13px;
      }

      .footer .social {
        display: flex;
        justify-content: flex-start;
        margin-bottom: 20px;
      }

      .footer .social a {
        margin-right: 20px;
      }

      .footer .visit {
        color: #eeeeee;
        font-size: 13px;
      }
    </style>
  </head>

  <body>
    <div class="container">
      <div class="logo">
        <img src="<%= url %>/logo.png" />
      </div>
      <p>Hello,</p>
      <p>
        Please click on the confirmation button or on the following link to
        confirm your email and activate your account.
      </p>
      <a class="link" href="<%= url %>/activate/<%= ticket %>"
        ><%= url %>/activate/<%= ticket %></a
      >
      <div class="confirm-container">
        <a class="confirm" href="<%= url %>/activate/<%= ticket %>"
          >Confirm email</a
        >
      </div>
      <hr />
      <p>Best, <br />The team</p>
      <hr />
      <div class="footer">
        <a class="visit" href="<%= url %>">Visit website</a>
        <p>©<%= new Date().getFullYear() %></p>
      </div>
    </div>
  </body>
</html>
